# Runtime matches a typical Databricks 14.x / 15.x cluster (Spark 3.5).
# Nothing else is added unless a later file justifies it in a comment.
pyspark==3.5.3
delta-spark==3.2.1
pytest==8.3.4
PyYAML==6.0.2
